import React from 'react';
import {ClipLoader} from "react-spinners";

const Initialize = () => {
    return (
        <div>
            <ClipLoader size={50}/>
        </div>
    );
};

export default Initialize;