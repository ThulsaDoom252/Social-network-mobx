import {Routes} from "../types";

export const routesConfig: Routes = {
    profileRoute: '/profile',
    rootRoute: '/',
    authRoute: '/auth',
    friendsRoute: '/friends',
    usersRoute: '/Users',
    infoRoute: '/info',
}